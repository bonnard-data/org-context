---
description: Validate the plugin builder installation by running diagnostics
---

Run this command and report the results:

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/pb" validate
```

If any checks failed, suggest how to fix them.
