---
description: Validate the org context installation by running diagnostics
---

Run this command and report the results:

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/oc" validate
```

If any checks failed, suggest how to fix them.
