{
  "description": "Plugin Builder — syncs role-specific content on session start",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/sync.mjs\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
